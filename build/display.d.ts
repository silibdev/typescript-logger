import { Level } from "./level";
export declare class Display {
    static msg(message: string, params: any[], moduleName: string, moduleColor: string, level: Level, moduleWidth: number, _console: any): void;
}
